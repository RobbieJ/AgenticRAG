# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Config & validation
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Anthropic Claude (0.107+ supports output_config structured outputs)
anthropic==0.107.0

# Retrieval (real, in-process vector search — no external services required)
numpy==2.2.0

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
